<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.coursemanager.mapper.CoursediscussMapper">
<!--  <insert id="save" parameterType="Coursenotice" >
     insert into coursenotice (courseid,noticetitle,noticecontext,byuser,userid,pushda_date) values(#{courseid},#{noticetitle},#{noticecontext},#{byuser},#{userid},#{pushdaDate})
 </insert>
 -->
 

    
     <select id="findbycourseid" parameterType="int" resultType="Coursediscuss">  
       select * from coursediscuss where belongcourseid = #{courseid} 
    </select> 
 </mapper>